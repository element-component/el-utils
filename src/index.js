import { on, off, once } from './event';
import { getStyle, setStyle } from './style';
import { hasClass, addClass, removeClass } from './class';

export default {
  on,
  off,
  once,
  getStyle,
  setStyle,
  hasClass,
  addClass,
  removeClass
};
